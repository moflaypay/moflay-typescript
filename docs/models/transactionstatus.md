# TransactionStatus

Current status. Possible values: initiating, pending, completed, failed, canceled, unknown

## Example Usage

```typescript
import { TransactionStatus } from "@moflay/sdk/models";

let value: TransactionStatus = "completed";
```

## Values

```typescript
"initiating" | "pending" | "completed" | "failed" | "canceled" | "unknown"
```