# ListCustomersSortOrder

The order in which to sort results. Defaults to `'desc'`. Use `'asc'` for ascending (oldest first) or `'desc'` for descending (newest first).

## Example Usage

```typescript
import { ListCustomersSortOrder } from "@moflay/sdk/models/operations";

let value: ListCustomersSortOrder = "desc";
```

## Values

```typescript
"asc" | "desc"
```